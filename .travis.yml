language: python
python:
  - "2.7"
env:
  - DJANGO_VERSION=1.8
# command to install dependencies
install:
  - pip install -q Django==$DJANGO_VERSION
  - pip install psycopg2
# command to run tests
script:
  - python EntHub/manage.py test
